# Student Work Area

Students must commit only this directory to their group fork.

Required Session 1 outputs include:

- `models/*best.pt`
- `models/*best.onnx`
- selected training/evaluation figures
- image and video validation outputs
- JSON/CSV summaries

The official notebook PDFs are submitted through Google Classroom and do not need to be committed unless the instructor explicitly requires it.
